/**
 * Default dog imagery: Stitch hero (silhouette / sunlit room), shared by onboarding photo slot,
 * home compact hero fallback, and the profile-setup banner.
 */
export const DEFAULT_DOG_PROFILE_IMAGE = require('@/assets/images/stitch-profile-setup-hero.png');
